import React from 'react';
import { products } from './constants/index'; // Corrected import



const ProductCart = ({imgURL, name, price}) => {
    return (
        <div> 
         l
        </div> 

    );
};

export default ProductCart;
